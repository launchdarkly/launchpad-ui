import { {{pascalCase name}} } from '../src';

describe('{{pascalCase name}}', () => {
  it('renders', () => {
    cy.mount(<{{pascalCase name}}>An important message</{{pascalCase name}}>);
    cy.getByTestId('{{dashCase name}}').should('be.visible');
  });

  it('is accessible', () => {
    cy.mount(<{{pascalCase name}}>An important message</{{pascalCase name}}>);
    cy.checkA11y();
  });
});
