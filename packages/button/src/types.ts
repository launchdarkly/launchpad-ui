enum ButtonType {
  BUTTON = 'button',
  SUBMIT = 'submit',
}

enum IconButtonSize {
  SMALL = 'small',
  NORMAL = 'normal',
}

enum EllipsisButtonType {
  VERTICAL = 'vertical',
  HORIZONTAL = 'horizontal',
  HAMBURGER = 'hamburger',
  DOWN = 'down',
  CHART = 'chart',
}

enum ButtonKind {
  DEFAULT = 'default',
  PRIMARY = 'primary',
  DESTRUCTIVE = 'destructive',
  MINIMAL = 'minimal',
  LINK = 'link',
  CLOSE = 'close',
}

enum ButtonSize {
  TINY = 'tiny',
  SMALL = 'small',
  NORMAL = 'normal',
  BIG = 'big',
}

enum ButtonGroupSpacing {
  COMPACT = 'compact',
  NORMAL = 'normal',
  LARGE = 'large',
}

export {
  ButtonType,
  EllipsisButtonType,
  ButtonKind,
  ButtonSize,
  IconButtonSize,
  ButtonGroupSpacing,
};
