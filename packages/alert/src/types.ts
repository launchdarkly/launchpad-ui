enum AlertKind {
  INFO = 'info',
  SUCCESS = 'success',
  WARNING = 'warning',
  ERROR = 'error',
  STRIPED = 'striped',
}

enum AlertSize {
  SMALL = 'small',
  MEDIUM = 'medium',
}

export { AlertKind, AlertSize };
