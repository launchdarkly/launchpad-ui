---
'@launchpad-ui/tokens': minor
---

Restore `brand.yellow` and `brand.green` to their original color values instead of aliasing them to `brand.lime`. The `federal` product background and the `yellow-*` gradients are also repointed back to yellow. The `brand.lime` tokens, lime gradients, and BadgeIcon `lime` variant introduced in #1907 remain available.
