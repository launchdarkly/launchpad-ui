---
---

Pinned devDependency (and internal-app dependency) version ranges to exact versions repo-wide, matching gonfalon's syncpack policy of fixed external dependency versions. No published package's runtime `dependencies` changed — this is tooling-only.
