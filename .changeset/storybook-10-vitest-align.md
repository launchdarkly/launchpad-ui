---
---

Align Storybook, vitest, and TypeScript devDependency versions with gonfalon ahead of the monorepo merge: Storybook 9 → 10.2.19 (all `@storybook/addon-*` packages, `@storybook/react-vite`, `storybook-addon-pseudo-states` → 10.2.19; `@storybook/addon-designs` → 11.1.4, the first stable release with Storybook-10 peer support), vitest/`@vitest/coverage-v8`/`@vitest/ui` → 4.1.10, and `typescript` → 5.9.2. No published package's `dependencies` or `peerDependencies` changed, so no version bump is needed.
