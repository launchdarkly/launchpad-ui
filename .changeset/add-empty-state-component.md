---
'@launchpad-ui/components': minor
'@launchpad-ui/icons': patch
---

Add `EmptyState` to display empty content views with an illustration, heading, description, and optional action button. Add `BadgeIconContext` so parent components can style `BadgeIcon` via context.
