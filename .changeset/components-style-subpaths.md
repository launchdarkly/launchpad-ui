---
'@launchpad-ui/components': minor
---

Add `./styles/base.css` and `./styles/themes.css` subpath exports so consumers, including this repository's own Storybook config, can import this package's base and theme stylesheets by package specifier instead of a path into its source tree.
