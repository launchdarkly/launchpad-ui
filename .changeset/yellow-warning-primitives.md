---
'@launchpad-ui/tokens': minor
'@launchpad-ui/components': patch
---

Add core `color.yellow` primitives and wire `feedback.warning` semantic aliases (bg, text, border, fill) in default and dark themes. Migrate Alert warning styles off temporary hardcoded colors to the new tokens.
