import { Box } from '@launchpad-ui/core';

export default function Index() {
  return (
    <Box bg={{ lightMode: '$black.100', darkMode: '$gray.700' }} color="$white.0">
      I am a box
    </Box>
  );
}
